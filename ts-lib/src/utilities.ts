export class utilities {
	public static add(x: number, y: number): number {
		return x + y;
	}

	public static subtract(x: number, y: number): number {
		return x - y;
	}

	public static say(message: string): string {
		return "say:" + message;
	}
}
